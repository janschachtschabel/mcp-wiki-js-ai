node_modules
.next
out
dist
build

# env / secrets
.env
.env.local
.env*.local

# vercel
.vercel

# OAuth session store (encrypted user JWTs + audit log) — NEVER commit.
# Local default is ./data when MCP_DATA_DIR is unset; the container uses /data.
data/

# misc
*.log
.DS_Store
tsconfig.tsbuildinfo
